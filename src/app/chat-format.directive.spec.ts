import { ChatFormatDirective } from './chat-format.directive';

describe('ChatFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatFormatDirective();
    expect(directive).toBeTruthy();
  });
});
